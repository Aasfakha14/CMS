<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Blog</title>
    <style>
        /* ---- GLOBAL ---- */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            line-height: 1.6;
        }

        /* ---- NAVBAR ---- */
        .navbar {
            width: 100%;
            background-color: #333;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar .logo {
            color: #ffcc00;
            font-size: 24px;
            font-weight: bold;
        }

        .navbar ul {
            list-style: none;
            display: flex;
        }

        .navbar ul li {
            margin-left: 25px;
        }

        .navbar ul li a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s;
        }

        .navbar ul li a:hover {
            color: #ffcc00;
        }

        /* ---- HERO SECTION ---- */
        .hero {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 100px 20px;
        }

        .hero h1 {
            font-size: 48px;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 20px;
        }

        /* ---- BLOG SECTION ---- */
        .container {
            width: 90%;
            max-width: 1200px;
            margin: 50px auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .card {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
            padding: 20px;
        }

        .card-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 8px;
            color: #333;
        }

        .card-subTitle {
            font-size: 14px;
            color: #888;
            margin-bottom: 12px;
        }

        .card-description {
            font-size: 16px;
            color: #555;
        }

        .card button {
            margin-top: 15px;
            padding: 10px 18px;
            border: none;
            border-radius: 5px;
            background-color: #ffcc00;
            color: #333;
            font-weight: bold;
            cursor: pointer;
        }

        .card button:hover {
            background-color: #e6b800;
        }

        /* ---- FOOTER ---- */
        .footer {
            background-color: #333;
            height: 40px;
            margin-top: 50px;
        }
    </style>
</head>
<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <div class="logo">My Blog</div>
        <ul>
            <li><a href="#">Home</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Contact</a></li>
        </ul>
    </div>

    <!-- HERO SECTION -->
    <div class="hero">
        <h1>Welcome to My Blog</h1>
        <p>Read the latest articles and insights on web development, design, and tech.</p>
    </div>

    <!-- BLOG CARDS -->
    <div class="container">
        <% if(blogs.length==0){ %>
            <h1>No Blogs</h1>
        <% } else { %>
            <% for (let i = 0; i < blogs.length; i++) { 
                let blog = blogs[i]; %>
                <div class="card">
                    <div class="card-title"><%= blog.title %></div>
                    <div class="card-subTitle"><%= blog.subTitle %></div>
                    <div class="card-description"><%= blog.description %></div>
                    <button>
                        <a href="/singlepage/<%- blog.id %>">Read More</a>
                    </button>
                </div>
            <% } %>
        <% } %>
    </div>

    <!-- FOOTER -->
    <div class="footer"></div>

</body>
</html>
